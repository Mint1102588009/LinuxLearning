#ifndef CALCULATESUB_H
#define CALCULATESUB_H

#include <Calculate.h>


class CalculateSub : public Calculate
{
    public:
        CalculateSub();
        virtual ~CalculateSub();
        virtual double getResult();
    protected:

    private:
};

#endif // CALCULATESUB_H
